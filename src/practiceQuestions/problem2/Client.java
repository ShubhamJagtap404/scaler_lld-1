package practiceQuestions.problem2;

/*
* Given a list of 100 URLs (e.g., "http://example.com/pageX"), simulate fetching
*   the page content using Thread.sleep(200ms).
    Implement two versions:
    Use a FixedThreadPool(10) to process 10 URLs at a time.
    Use a CachedThreadPool to process them dynamically.
    Compare the execution time for both approaches.
* */
public class Client {
}
